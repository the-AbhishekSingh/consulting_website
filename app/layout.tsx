import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Mukio - Helping People Achieve All Their Financial Goals",
  description:
    "Leverage the power of data to drive smarter financial decisions. Our advanced analytics solutions help businesses uncover insights, optimize performance, and stay ahead of the competition.",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
